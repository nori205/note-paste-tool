# note貼り付け整形ツール ─ 設計メモ

## これは何か
下書き（記法つきテキスト）を貼って「コピー」ボタンを押すと、note向けのHTMLを
クリップボードに書き込むツール。noteに貼ると、見出し・太字・リスト・引用・リンクが
崩れず、しかも「余計な空行」が出ない状態で入る。単一HTMLファイル・スマホ完結・
インストール不要（GitHub Pagesで公開する想定）。

## なぜ作るか（既存のやり方との違い）
今までの「VSCodeで書く → noteに貼って純正のマークダウン変換に任せる」だと、
- PCが要る
- note純正変換のクセで段落間に絶妙な空行が入って読みにくい

このツールは note純正変換を使わず、**自前でキレイなHTMLをクリップボードに直接書き込む**。
スマホだけで完結し、隙間問題も自前で潰す。作者自身が一番のユーザー。

## 仕組みの要点（2026-08-11 実機検証で判明した仕様に更新）
- ~~当初案：`navigator.clipboard.write()` で `text/html` を書き込み、自前でHTMLを組み立てて渡す~~
  → **実機（note公式Androidアプリ）で検証した結果、この方式はNG**。
  note公式アプリは外から渡された `<h2>`/`<blockquote>` などのタグを一切信用せず、
  中身のテキストだけを抜き出して1行ずつ別ブロックに分解してしまう
  （太字・箇条書き・リンクはたまたま見た目が近くなるので誤魔化せていたが、
  見出しと引用で完全に破綻することが分かった＝偶然の一致だった）。
- **現在の方式**：`navigator.clipboard.writeText()` で `text/plain` のみを書き込む。
  中身は下書きの記法（`## ` `**太字**` `- ` `> ` `[]()`）をそのまま残したテキスト。
  note公式アプリは**貼り付けられたプレーンテキストを自前のマークダウン変換にかけて
  見出し・引用・太字・箇条書き・リンクを正しく生成する**（実機検証で確認済み）。
  → つまりこのツールの役割は「HTMLを組み立てて運ぶ」ことではなく、
    「noteが正しく認識する記法・改行構造に整えたテキストを渡すだけ」に変わった。
- **空行の有無だけで詰め方が変わる**（実機検証で確認）：
  - ふつうの改行（空行なし）で区切ると→**詰まって表示される**
  - 空行（`\n\n`）で区切ると→段落として余白が入る
  - HTMLの`<p>`タグで分けても、note側では空行区切りと同じ扱いになり余白が入る
    （＝`<br>`/`<p>`の使い分けというアイデアは実機では機能しない。
    改行1つ・空行という「テキストの見た目」だけで制御する）

## 対応記法
- `## ` → h2見出し / `### ` → h3見出し（noteはh2/h3のみ対応）
- `- ` `*` `・` → 箇条書き(ul) / `1. ` → 番号つき(ol)
- `> ` `＞` → 引用(blockquote)
- `**太字**` → strong
- `[表示](URL)` → リンク
- `【埋め込み】URL` → 埋め込みたい場所の目印。**カード化はnote側で手動**
  （noteの埋め込みはエディタにURLを貼った瞬間にnoteサーバーが生成する仕組みで、
   外からHTMLで持ち込めない。だから場所とURLだけ準備して、最後の1手は手動）
- 空行＝段落区切り / ふつうの改行＝行内改行

## 役割分担（重要）
- **アプリの仕事**：書式を崩さず運ぶ＋余計な空行を出さない。
- **AI（スキル）の仕事**：文章そのものを、のりのスタイルで書く
  （句点で改行・シーンの変わり目で空行）。これはアプリではなく下書き生成側の担当。

## Claude Codeでやる仕上げ（TODO）
1. ~~実機確認（最優先）~~ → **2026-08-11 完了**。note公式Androidアプリで6項目
   （太字/見出し/箇条書き/引用/リンク/空行の詰め方）を診断ページ（`debug.html`）で
   個別検証。結果、`text/html`書き込み方式は見出し・引用が崩れることが判明したため、
   `text/plain`のみ書き込み、note自身のマークダウン変換に委ねる方式に変更した。
2. ~~ClipboardItemのtext/html対応確認~~ → 対応確認は不要になった（text/htmlを
   使わない方針にしたため）。`navigator.clipboard.writeText()`はClipboardItemより
   対応環境が広く、iOS Safari / Android Chromeどちらでも通る想定。
3. ~~`<br>`の扱い~~ → 検証の結果、`<br>`/`<p>`の使い分けというアイデア自体が
   note公式アプリでは機能しないことが判明（HTMLタグはnote側で信用されない）。
   詰め方は「ふつうの改行＝詰まる／空行＝段落の余白」という**プレーンテキストの
   見た目そのもの**で制御する方式に置き換えた。
4. **残タスク**：フルの下書き（見出し+太字+リスト+引用+リンクを含む文章）を
   新方式でコピー→note公式アプリに貼り付け、一括での崩れがないか再確認する。
   個別要素は通ったが、複数要素が混在したときの挙動は未検証。
5. 余力があれば：入力のローカル保存、記法早見表の充実、貼り付け前後の比較表示など。

## ファイル
- `index.html` … 本体（これ一つで動く）
- `debug.html` … note公式アプリの貼り付け挙動を切り分けるための診断ページ
  （太字/見出し/箇条書き/引用/リンク/空行の詰め方を個別にテストできる）
