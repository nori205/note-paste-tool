# note貼り付け整形ツール ─ 設計メモ

## これは何か
下書き（記法つきテキスト）を貼って「コピー」ボタンを押すと、note向けのHTMLを
クリップボードに書き込むツール。noteに貼ると、見出し・太字・リスト・引用・リンクが
崩れず、しかも「余計な空行」が出ない状態で入る。単一HTMLファイル・スマホ完結・
インストール不要（GitHub Pagesで公開する想定）。

## なぜ作るか（既存のやり方との違い）
今までの「VSCodeで書く → noteに貼って純正のマークダウン変換に任せる」だと、
- PCが要る
- note純正変換のクセで段落間に絶妙な空行が入って読みにくい

このツールは note純正変換を使わず、**自前でキレイなHTMLをクリップボードに直接書き込む**。
スマホだけで完結し、隙間問題も自前で潰す。作者自身が一番のユーザー。

## 仕組みの要点
- `navigator.clipboard.write()` で `text/html` と `text/plain` の両方を書き込む。
  noteエディタが text/html を拾えば書式が保持される。
- 出力HTMLは**ブロックタグの間に改行・空白を一切入れない**（余計な余白の元を断つ）。
- 段落内の改行は `<br>`（詰まる）、段落の区切りは `<p>`（余白が入る）で表現。
  → 書き手（のり）のスタイル「句点で改行・シーンの変わり目で空行」がそのまま
    「文内は詰まる・シーンで余白」に対応する。

## 対応記法
- `## ` → h2見出し / `### ` → h3見出し（noteはh2/h3のみ対応）
- `- ` `*` `・` → 箇条書き(ul) / `1. ` → 番号つき(ol)
- `> ` `＞` → 引用(blockquote)
- `**太字**` → strong
- `[表示](URL)` → リンク
- `【埋め込み】URL` → 埋め込みたい場所の目印。**カード化はnote側で手動**
  （noteの埋め込みはエディタにURLを貼った瞬間にnoteサーバーが生成する仕組みで、
   外からHTMLで持ち込めない。だから場所とURLだけ準備して、最後の1手は手動）
- 空行＝段落区切り / ふつうの改行＝行内改行

## 役割分担（重要）
- **アプリの仕事**：書式を崩さず運ぶ＋余計な空行を出さない。
- **AI（スキル）の仕事**：文章そのものを、のりのスタイルで書く
  （句点で改行・シーンの変わり目で空行）。これはアプリではなく下書き生成側の担当。

## Claude Codeでやる仕上げ（TODO）
1. **実機確認（最優先）**：スマホのnoteアプリ／ブラウザで、見出し・太字・リスト・引用・
   リンクがちゃんと保持されるか。特に「段落間の空行が二重にならないか」を目視。
   → 崩れたら、出力HTMLの詰め方（<br>/<p>の使い分け、余白の持たせ方）を調整。
2. iOS Safari / Android Chrome で `ClipboardItem` の text/html 書き込みが通るか確認。
   通らない端末があればフォールバック挙動を検討。
3. noteが `<br>` をどう扱うか実機で確認。もし `<br>` が段落扱いされて隙間が出るなら、
   段落内改行の方式を変える（例：全角スペース＋改行、または連結して1行にする等）。
4. 余力があれば：入力のローカル保存、記法早見表の充実、貼り付け前後の比較表示など。

## ファイル
- `index.html` … 本体（これ一つで動く）
